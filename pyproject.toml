[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "saaschurn"
version = "0.1.0"
description = "SaaS Churn CLI"
requires-python = ">=3.9"
dependencies = [
    "rich",
    "requests",
    "python-dotenv",
]