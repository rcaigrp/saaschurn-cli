[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends"

[project]
name = "saaschurn"
version = "0.1.0"
dependencies = [
    "rich",
    "requests",
    "python-dotenv"
]

[project.scripts]
saaschurn = "saaschurn.cli:main"
